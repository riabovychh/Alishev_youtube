package interfaces;

// по сути интерфейсы используются для того, чтоб быть уверенным, что в каком-то классе будут все необходимые методы
// это как темплейт, в котором точно есть все поля, которые надо заполнить
public interface Info {
    public void showInfo();
}
